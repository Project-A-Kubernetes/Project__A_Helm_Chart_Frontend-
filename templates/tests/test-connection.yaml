apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "project_A_helm_frontend.fullname" . }}-test-connection"
  labels:
    {{- include "project_A_helm_frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "project_A_helm_frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
